{
  "name": "health-tracker-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"sync/*.test.mjs\"",
    "sync": "node --env-file=.env sync-to-supabase.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
